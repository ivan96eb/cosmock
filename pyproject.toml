[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cosmock"
version = "0.0.1"


dependencies = [
    "numpy>=1.23",
    "scipy>=1.9",
    "joblib>=1.2",
    "healpy>=1.16",
]
