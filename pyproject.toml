[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cosmock"
version = "0.0.1"


dependencies = [
    "numpy>=2.4",
    "scipy>=1.17",
    "joblib>=1.5",
    "healpy>=1.19",
]
