Big picture ideas of what functions/etc we need to add

1: Function to do the actual transform

- Gn(x, N, lambda) -> delta

2a: Function that fits for the parameters of the transform (lambda)

fit_Gn_to_map(delta, N) -> lambda

2b: Function that gives us C^x given C^delta 

Cl_delta_to_Cl_gauss(Cl_delta, N, lambda) -> Cl_gauss

3: Function that creates an mock given C^x and lambda

mock_delta(seed, N, lambda, Cl_gauss) -> delta_mock



Wrap the general workflow to organize it nicely as follows:

delta_params = fitter(delta, Cl_delta) -> NamedTuple(lambda, Cl_gauss, N)
mocks = generate_mocks(delta_params, num_mocks, seed) 